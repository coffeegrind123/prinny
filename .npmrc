legacy-peer-deps=true
save-exact=true

# Dependency lifecycle scripts (preinstall/install/postinstall) execute arbitrary
# code from every transitive package at install time. This tree is built by CI
# jobs that hold repository write credentials, so that code must not run there.
# Matches the policy in the parent repo's .npmrc.
ignore-scripts=true
